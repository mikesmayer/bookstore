%p#notice= notice
%h1 Listing Categories
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    = render @categories
%br/
- if can? :manage, :category
  = link_to 'New Category', new_category_path
