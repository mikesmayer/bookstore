%p#notice= notice
%h1 Listing Orders
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    -# =@orders.where(status: 'initializing').inspect
    = render partial: 'order', collection: @orders.where(status: 'creating')

