%h5.center-align Ordered books
%table
  %thead
    %tr
      %th= "Book author"
      %th= "Book title"
      %th= "Book price"
      %th= "Book quantity"
  %tbody
    - cart_books.each do |book|
      %tr
        %td= book["author"]
        %td= book["title"]
        %td= book["price"]
        %td= book["quantity"]
        - unless book["id"].nil?
          %td= link_to "Show" , book_path(Book.find(book["id"]))
          %td= link_to "Delete",delete_from_cart_book_path(Book.find(book["id"])),
                                method: :delete, remote:true
%ul.right-align.cart-order-panel
  %h5 Total price:
  %li
    = cart_total_price
  %li
    = link_to "Create Order", new_order_path(@order), class: "waves-effect waves-light btn "